syntax = "proto3";

package protobuf;

enum MessageType {
  UNKNOWN_MESSAGE_TYPE = 0;
  ONE_TO_ONE = 1;
  PUBLIC_GROUP = 2;
  PRIVATE_GROUP = 3;
  // Only local
  SYSTEM_MESSAGE_PRIVATE_GROUP = 4;
}

enum ImageType {
  UNKNOWN_IMAGE_TYPE = 0;
  PNG = 1;
  JPEG = 2;
  WEBP = 3;
  GIF = 4;
}
