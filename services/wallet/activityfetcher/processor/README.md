# processor

This module is in charge of processing externally fetched transactions to build a table of activity entries.
Changes to the processing algorithm might require re-processing the list of transactions.
